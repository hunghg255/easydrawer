// Main entrypoint for the bundler (ESBuild/Webpack/etc.) when creating the bundled
// portion of a release.

// import '../styles';
import Editor from './draw/Editor';

export * from './draw/lib';

export default Editor;
console.log('Hello Editor 111');
